# ADR-017 Local Validation Report - Issues #124, #125, #127, #128, #129

Date: 2026-08-06

Environment:

- Local Docker Compose only
- Frontend: `http://localhost:3000`
- Backend: `http://localhost:3001`
- Database: local PostgreSQL 16
- Mail provider: sandbox/mock only
- Data: synthetic seed only

This report does not represent Staging or Production deployment approval.

## Automated checks

| Check | Result |
|---|---|
| Prisma schema validation | Pass |
| Backend lint and production build | Pass |
| Backend Jest | Pass: 24 suites / 170 tests |
| Frontend lint and production build | Pass |
| Frontend Vitest | Pass: 6 files / 43 tests |
| `git diff --check` | Pass; line-ending warnings only |

## Local Compose and migration checks

- `docker compose up -d --build`: Pass; frontend, backend, and PostgreSQL healthy.
- `npm run db:deploy` inside the backend container: Pass; 14 migrations found and no pending migration.
- `npm run local:seed` inside the backend container: Pass twice, confirming idempotent synthetic seed behavior.

## ADR-017 API smoke

The local container smoke verified this sequence:

1. A mapped scan created a mail job in `waiting`.
2. Cancellation before the database deadline returned `canceled`.
3. The canceled job did not invoke the sandbox provider.
4. A new idempotency key allowed an immediate rescan.
5. The rescan remained waiting until due and then reached `sent` through the worker.

Result: Pass (`waiting -> canceled`, then rescan `waiting -> sent`).

Read-only database evidence confirmed the canceled synthetic job had 0 delivery attempts and 0 provider invocations; the sent rescan had exactly 1 invoked attempt and `send_not_before <= sent_at`.

## Browser validation

Using the in-app browser against the local Compose frontend:

1. Logged in with a synthetic local operator.
2. Submitted a synthetic `V2E` action code.
3. Confirmed an enabled `取消发送 (10s)` button appeared on the new row.
4. Canceled the send and confirmed the row showed `已取消`.
5. Reloaded the page and confirmed `已取消` persisted and no cancel button remained.

Result: Pass.

## Scope boundary

- No real mail provider was configured or called.
- No customer data, real email address, credential, token, or message body is recorded here.
- No Staging or Production action was performed.
